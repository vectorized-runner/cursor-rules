---
description: Wire contract DTOs live under a Contracts/ folder and stay pure data — no display logic, no UI references, no client-convenience fields
globs: **/Contracts/**/*.cs
alwaysApply: false
---

# Pure DTO Contracts

**Wire DTOs live under a folder named `Contracts/`** (any depth, any assembly), and nothing else goes in that folder. That folder is the **wire contract** — the serialized shape exchanged with the backend. Its types must stay **pure data**. Do not add client-side, presentation, or app-specific logic to them.

## Allowed in contracts

- Serialized fields with `[JsonProperty(...)]` (and `[Serializable]`, enum `[EnumMember(...)]`)
- Enum definitions and their wire-name attributes
- XML doc comments and `[JsonIgnore]` on serialization-only concerns

## Not allowed in contracts

Put these on a **client system** (a private helper on the consumer, or a static helper next to it) instead:

- Display/formatting logic — labels, hierarchy names, UI strings (`DisplayName`, `ToLabel()`)
- Derived presentation values — colors, sprites, icons, sort keys for UI
- Client-only computed properties encoding app behavior rather than wire shape
- Any reference to `UnityEngine.UI`, TMP, sprites, materials, or scene objects

```csharp
// ❌ BAD — client-side label logic on the contract
// Contracts/Domain/Card.cs
public string DisplayName => Value switch { CardValue.Wild => "Wild", _ => $"{Color}_{Value}" };

// ✅ GOOD — private helper on the caller
// CardFactory.cs (the consumer, outside Contracts/)
private static string FormatCardName(Card card) => card.Value switch
{
    CardValue.Wild => "Wild",
    _ => $"{card.Color}_{card.Value}",
};
```

## Gray area: wire-derived helpers

Small `[JsonIgnore]` helpers that only restate the wire data (e.g. `Card.IsWild` — fixed by game rules, not the client) are tolerated as part of the data definition. If the value depends on **how the client presents or names things**, it belongs on a client system.

## Changing the contract

When adding or changing wire types, match the backend spec exactly (field names, casing, optionality, and **which payload object owns the field**) and update only the contract folder — mapping/enrichment happens in gameplay systems, not in the DTO.

### No client-convenience fields

- Never add a serialized DTO field solely because client gameplay or presentation needs a convenient flag/value.
- A wire field belongs only on the exact DTO whose backend payload object defines it. Do not copy an envelope, parent, sibling, or differently shaped action field onto another DTO.
- Do not enrich a DTO in a mapper with fields that are absent from that DTO's canonical backend schema, even when the source value exists elsewhere in the same publication.
- Before changing a DTO, verify its exact owner and shape in the project's backend spec (see its `AGENTS.md`). The presence of a field somewhere in a publication does not authorize adding it to every nested/action-specific DTO.
- Derive client state outside the `Contracts/` folder, using the authoritative gameplay state or the payload object that already owns the value.

```csharp
// ❌ BAD — game_over belongs to the enclosing action payload; this copy exists only for presentation
[JsonProperty("game_over")]
public bool IsGameOver;

// ✅ GOOD — presentation derives the winning play from authoritative post-update gameplay state
bool isWinningPlay = playerCardCounts.TryGetValue(data.PlayerId, out int count) && count == 0;
```

## Agent checklist

- [ ] Wire types live under a `Contracts/` folder; non-wire types do not
- [ ] No display/formatting/UI logic added to contract types
- [ ] Derived label/visual helpers live on a client system, not the DTO
- [ ] Contract types reference no presentation namespaces or Unity UI types
- [ ] New computed members reflect wire/game data, not client presentation choices
- [ ] Every new serialized field is defined on this exact payload object in the backend spec
- [ ] No DTO field was copied/enriched from an envelope or another payload for client convenience
